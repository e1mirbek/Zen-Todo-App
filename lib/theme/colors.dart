import 'package:flutter/material.dart';

class AppColors {
  // Основной фон
  static const Color background = Color(0xFFFFFFFF);

  // Текст
  static const Color mainText = Color(0xFF1D1D2B);
  static const Color secondaryText = Color(0xFF7A7A85);

  // Акцентные цвета (Кнопки и карточки)
  static const Color primaryPurple = Color.fromRGBO(77, 73, 225, 1.0);
  static const Color bgCard = Color.fromRGBO(255, 255, 255, 1.0);
  static const Color red = Colors.red;
  // Цвет кнопки "Создать"
  static const Color darkPurple = Color(0xFF4A3780); // Цвет иконки "+",
  static const Color bgField = Color.fromRGBO(248, 250, 252, 1.0);

  // Статусы (для карточек статистики)
  // static const Color cardBlueBg = Color(0xFFEFELFF);
  static const Color cardBlueText = Color(0xFF6F74EB);
  static const Color cardBlueBg = Color(0xFFEFEFFF);

  static const Color cardGreenBg = Color(0xFFE6F9F0);
  static const Color cardGreenText = Color(0xFF00C48C);

  static const Color cardOrangeBg = Color(0xFFFFF8E5);
  static const Color cardOrangeText = Color(0xFFFFA800);
}
